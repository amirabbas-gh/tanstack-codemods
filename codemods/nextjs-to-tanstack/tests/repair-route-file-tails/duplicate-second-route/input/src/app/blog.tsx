import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/blog")({
  component: Blog,
});

          </section>


        </>
      )}
    </section>
  );
}

export const Route = createFileRoute("/blog")({
  component: Blog,
});
